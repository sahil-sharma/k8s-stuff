apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: mtls-disable
  namespace: tempo
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: tempo
  mtls:
    mode: DISABLE
