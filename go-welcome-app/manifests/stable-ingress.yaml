---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: welcome-app-stable-ingress
  namespace: welcome-app
spec:
  ingressClassName: nginx
  rules:
    - host: welcome.local.io
      http:
        paths:
          - path: /welcome
            pathType: Prefix
            backend:
              service:
                name: welcome-app-stable
                port:
                  number: 80
          - path: /external
            pathType: Prefix
            backend:
              service:
                name: welcome-app-stable
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: welcome-app-stable
                port:
                  number: 80
