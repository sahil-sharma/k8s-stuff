apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: gateway
  namespace: argo-workflows
spec:
  selector:
    app: istio-gateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - jobs.local.io
