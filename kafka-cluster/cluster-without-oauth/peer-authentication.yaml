apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: kafka-internal-mtls-disable
  namespace: kafka-cluster
spec:
  selector:
    matchLabels:
      strimzi.io/cluster: my-cluster
  mtls:
    mode: DISABLE
