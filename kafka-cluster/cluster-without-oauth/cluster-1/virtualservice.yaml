apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: kafka-gateway
  namespace: cluster-1
spec:
  selector:
    app: istio-gateway
  servers:
    - port:
        number: 9092
        name: tcp-kafka
        protocol: TCP # CRITICAL: Must be TCP
      hosts:
        - "*"
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: kafka-vs
  namespace: cluster-1
spec:
  hosts:
    - "*"
  gateways:
    - kafka-gateway
  tcp: # CRITICAL: Use 'tcp' block, NOT 'http'
    - match:
        - port: 9092
      route:
        - destination:
            host: cluster-1-kafka-bootstrap.cluster-1.svc.cluster.local
            port:
              number: 9092
