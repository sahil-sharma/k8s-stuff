apiVersion: v1
kind: ServiceAccount
metadata:
  name: eso-kafka-reader
  namespace: kafka-ui
---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: kafka-cluster-store
  namespace: kafka-ui
spec:
  provider:
    kubernetes:
      remoteNamespace: kafka-cluster
      auth:
        serviceAccount:
          name: eso-kafka-reader
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kafka-ui-kafka-creds
  namespace: kafka-ui
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: kafka-cluster-store
    kind: SecretStore
  target:
    name: kafka-ui-kafka-creds
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: kafka-ui
        property: password
